# Cadence-Synora
 The problem we focused on is what happens when a task is assigned to a worker, but that worker suddenly becomes unavailable. Instead of allowing the task to remain stuck, CADENCE detects worker inactivity and automatically puts the task back into the queue so another available worker can continue the work.
